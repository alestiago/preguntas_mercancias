name: pm_questions_bank
description: Dart models and question bank support for the preguntas mercancias app.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  collection: ^1.19.1
  flutter:
    sdk: flutter
  json_schema_builder: ^0.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/pm_260326/
