name: pm_questions
description: Pure Dart question domain and ingestion APIs.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  equatable: ^3.0.0
  json_schema_builder: ^0.1.6

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.31.1
